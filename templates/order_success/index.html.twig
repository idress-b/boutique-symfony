{% extends 'base.html.twig' %}

{% block title %}Confirmation de commande{% endblock %}

{% block content %}
    <h2>Confirmation de commande</h2>
    <p>Bonjour <strong>{{ order.user.firstName }}  {{ order.user.lastName }}</strong>,</p>
    <p>Nous vous remercions pour votre commande <strong>n°{{ order.reference }}</strong></p>
    <p>Une confirmation vient de vous être envoyé par e-mail à l'adresse <strong>{{ order.user.email }}</strong></p>
    <hr>
    Votre commande sera livrée par <strong>{{ order.carrierName }}</strong> à l'adresse suivante : <br/>
    <p>{{ order.delivery|raw }}</p>
    <hr>
    Pour suivre votre commande, rendez-vous dans votre <a href="{{ path('account_order') }}">compte</a>.

{% endblock %}
